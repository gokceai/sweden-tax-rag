# Model and embedding stack
# CPU-only build for HuggingFace Spaces. No CUDA index needed.
# These exact versions are verified to work together (tested locally).
torch==2.5.1
transformers==5.5.0
sentence-transformers==5.3.0
