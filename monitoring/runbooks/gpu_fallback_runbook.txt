Sweden Tax RAG Incident Runbook: GPU Not Used / CPU Fallback

1) Confirm runtime has CUDA torch
- python -c "import torch; print(torch.__version__, torch.version.cuda, torch.cuda.is_available())"
- Expected: cuda_available=True

2) Confirm app environment
- Ensure Streamlit/API is launched from project venv (.taxtenv)
- Mismatch between global python and venv causes CPU fallback

3) Confirm model device
- sentence-transformers model should initialize on cuda:0
- Monitor with nvidia-smi during real query load

4) Confirm exporter metrics (if enabled)
- Prometheus target dcgm-exporter should be UP
- Grafana GPU dashboard should show non-zero util during embedding/generation

5) Common fixes
- Install CUDA-enabled torch in project venv
- Restart app after environment changes
- Ensure no CPU-only torch wheel remains in active venv

6) Validate
- Run a retrieval query
- Check nvidia-smi GPU util and memory usage increase
